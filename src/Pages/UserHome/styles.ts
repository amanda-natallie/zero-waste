import { Card } from '@vallorisolutions/foa-design-system';
import styled from 'styled-components';

export const CustomCard = styled(Card)`
    margin: 0 auto;
    width: 95%;

    & > div {
        margin-top: -40px;
    }
`;
